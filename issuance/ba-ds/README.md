## BA/DS Certificate Issuance

Use one folder per cohort.

Suggested pattern:

- `cohort-1`
- `cohort-2`
- `cohort-3`

Each cohort folder should contain:

- `recipients.csv` for names and certificate metadata
- `exports/` for generated certificate files

